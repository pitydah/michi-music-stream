{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://michi.link/schemas/sync-manifest.schema.json",
  "title": "SyncManifest",
  "description": "Full library manifest used for initial synchronisation or full resync",
  "type": "object",
  "required": ["cursor", "generated_at", "tracks"],
  "additionalProperties": false,
  "properties": {
    "cursor": {
      "type": "string",
      "description": "Opaque cursor token for the first delta request",
      "minLength": 1,
      "examples": ["initial_cursor_a1b2c3d4"]
    },
    "generated_at": {
      "type": "string",
      "description": "ISO 8601 timestamp when this manifest was generated",
      "format": "date-time",
      "examples": ["2026-06-29T12:00:00Z"]
    },
    "tracks": {
      "type": "array",
      "description": "Full list of track objects in the library",
      "items": {
        "$ref": "track.schema.json"
      }
    },
    "albums": {
      "type": "array",
      "description": "Full list of album objects in the library",
      "items": {
        "$ref": "album.schema.json"
      }
    },
    "artists": {
      "type": "array",
      "description": "Full list of artist objects in the library",
      "items": {
        "$ref": "artist.schema.json"
      }
    },
    "playlists": {
      "type": "array",
      "description": "Full list of playlist objects in the library",
      "items": {
        "$ref": "playlist.schema.json"
      }
    },
    "artworks": {
      "type": "array",
      "description": "List of artwork references with content hashes",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "md5"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "md5": { "type": "string", "pattern": "^[a-fA-F0-9]{32}$" }
        }
      }
    }
  }
}
